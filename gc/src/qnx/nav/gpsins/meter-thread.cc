/* -*- indent-tabs-mode:T; c-basic-offset:8; tab-width:8; -*- vi: set ts=8:
 *
 * Khian Hao Lim
 * Oct 6 03
 * Overbot
 *
 * The thread that reads from meter server for odometry data
 */

#include "thread.h"

//TODO
//static char * meter_server_name = "METERSERVER";

void *
meter_thread(void * args_v)
{
	
	//printf("Starting meter-thread\n");

	//Kalman_Filter * kf = (Kalman_Filter*) args_v;	

//	MsgClientPort clientport(meter_server_name,0.2);

	//while (true) {
	//	sleep(1);
//		//some msg type
//		int err = clientport.MsgSend(/* some msg */);
//		if (err) {
//			perror("Sending Req to Meter Server\n");
//			sleep (1);
//			continue;
//		}

//		if (verbose) {
//			printf("Got reply from Meter Server\n");
//		}
	//}
	
}
