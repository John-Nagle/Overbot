Watchdog

26 JAN 2004 -- J. Nagle

Starting programs works fine, even across the network.
But the "watchdog" doesn't actually perform any
"watchdogging" functions.  It will never kill a program or force a reboot.
Some code to do that is in, but unused.